/**
 * const    : [예약어]
 * maxLevel : [식별자], or 앞에 const, let과 함께 쓰여진다면 [변수명] 이라고도 한다.
 * =        : 연산자(할당 연산자)
 * 99       : 값
 * 99 + 1   : 표현식 (표현식은 무조건 값으로 도출이 된다!)
 * ;        : 세미콜론(코드의 끝)
 */
const maxLevel = 99 + 1;

/**
 * function: [키워드]
 * sum: [식별자], or 앞에 function와 함께 쓰여진다면 [함수명] 이라고도 한다.
 */
function sum() {}

// 이처럼, 식별자는 앞에 오는 키워드에 따라서 다른 이름으로 불기도 한다.

// QUESTION. var, let, const 는 어떤 차이가 있을까?
